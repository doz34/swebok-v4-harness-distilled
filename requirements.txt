# SWEBOK v4 Harness V2 — Base dependencies
# Pure stdlib by default (zero install for the deterministic L0 path).

# Optional: real LLM providers (install only what you need)
# pip install openai          # for OpenAIProvider
# pip install anthropic       # for AnthropicProvider
# pip install sentence-transformers  # for SentenceTransformerProvider

# Development / testing
# pip install pytest          # for unit tests
# pip install hypothesis      # for property-based tests
# pip install ruff            # for linting
# pip install bandit          # for security linting

# scikit-learn is required by scripts/theme_clusterer.py (the v2 corpus-enrichment
# pipeline). It is opt-in: the deterministic L0 path (compiled_knowledge.py) does
# not import sklearn. Install only if you plan to re-run the v2 enrichment.
# scikit-learn>=1.3,<2.0
