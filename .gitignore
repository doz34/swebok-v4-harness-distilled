# Runtime state (per-project, regenerated on every install)
.swebok_state.db
state.db
*.db
*.db.bak.*
*.db.corrupt.*
*.db.pre-rebuild.*

# HMAC secret (regenerated on first install; NEVER commit)
.audit_key

# Build artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Generated by retrieval pipeline (regenerated by CI; do not commit)
index.json
intent-map.json

# Distilled corpus artifacts (research-only; not loaded at runtime)
distilled_corpus/
distilled_corpus_v2/
coverage_report/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~
scripts/retrieval/

# Per-machine rendered settings (use settings.template.json as canonical)
settings.json

# Open-access PDFs (untracked per project decision 2026-06-08)
audit/corpus-references/downloads/
audit/corpus-crawler/downloads/
audit/p10-demo/
